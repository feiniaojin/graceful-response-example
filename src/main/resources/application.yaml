server:
  port: 9090

global:
  result:
    handler:
      print: true

spring:
  mvc:
    throw-exception-if-no-handler-found: true
    static-path-pattern: "*.html"
    add-mappings: false
    view:
      suffix: .html
      prefix: /

logging:
  level:
    cm.gingo: DEBUG
